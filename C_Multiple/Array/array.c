#include <stdio.h>
#include "array.h"

bool Insert(SqList *L, int i, ElemType e){
    int k;
    if(L->length == MAXSIZE)
    return false;
    if(i<1 || i > L->length+1)
    return false;
    if(i<L->length )
    {
        for()
    }
 
}